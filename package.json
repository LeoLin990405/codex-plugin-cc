{
  "name": "leo-cc-plugins",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-model plugin marketplace for Claude Code — Codex + CN Models.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "prebuild": "mkdir -p plugins/codex/.generated/app-server-types && codex app-server generate-ts --out plugins/codex/.generated/app-server-types",
    "build": "tsc -p tsconfig.app-server.json",
    "test": "node --test tests/*.test.mjs",
    "cn:setup": "node plugins/cn/scripts/cn-companion.mjs setup"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  }
}
